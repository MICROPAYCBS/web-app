# User-guide offline build (retired)

The operator handbook now lives in the sibling **Micropay CBS Docs** repo (`documentation`).

This tooling folder is kept only as a historical reference. Prefer:

```bash
cd ../documentation   # or your clone of micropay/documentation
pnpm install
pnpm run dev          # or pnpm run build
```

Do not add new offline-zip features here.
