# Route inventory tooling

Future script: scan `reference/web-app` `*-routing.module.ts` files and emit `docs/parity/routes.json` for automated parity tracking.

```bash
# Planned
pnpm run routes:inventory
```
