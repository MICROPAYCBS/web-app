# Fineract rules extractor

Future tooling to draft `packages/validation/manifests/*.json` from Fineract Java:

- Bean Validation annotations (`@NotNull`, `@Size`, …)
- `PlatformApiDataValidationException` usages in command handlers

Output requires human review before merge.
